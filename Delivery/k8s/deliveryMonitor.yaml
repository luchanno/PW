apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: delivery
  namespace: msusers-ns
  labels:
    app: delivery
spec:
  endpoints:
    - port: metrics
      scrapeTimeout: 30s
      path: "/actuator/prometheus"     
#  jobLabel: stack
  selector:
    matchLabels:
      app: delivery
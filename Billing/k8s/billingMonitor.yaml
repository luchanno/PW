apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: billing
  namespace: msusers-ns
  labels:
    app: billing
spec:
  endpoints:
    - port: metrics
      scrapeTimeout: 30s
      path: "/actuator/prometheus"     
#  jobLabel: stack
  selector:
    matchLabels:
      app: billing